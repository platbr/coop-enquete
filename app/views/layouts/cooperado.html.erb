<!DOCTYPE html>
<html>
  <head>
    <title>COOHSEP - Deliberação Coletiva</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'cooperado', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'cooperado', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <div class="container-sm">
    <% if @current_user %>
    <nav class="navbar navbar-expand-lg navbar-light">
    <div class="container-fluid">
      <a class="navbar-brand" href="#">COOPSEP - Deliberação Coletiva</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
        <div class="navbar-nav">
          <%= nav_link("Início", "/") %>
          <%= nav_link("Anteriores", "/enquetes?status=concluida") %>
          <%= nav_link("Sair (#{@current_user.short_name})", "/logout") %>
        </div>
      </div>
    </div>
  </nav>
      <% end%>
      <%= yield %>
      <%= toastr_flash %>
    </div>
  </body>
</html>
